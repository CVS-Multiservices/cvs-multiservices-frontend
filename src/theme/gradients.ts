import { COLORS } from './colors';

export const GRADIENTS = {
  mainBg: `linear-gradient(135deg, ${COLORS.primary}, ${COLORS.secondary})`,
  gold: `linear-gradient(90deg, ${COLORS.accent}, ${COLORS.accentLight})`,
  overlay: `linear-gradient(180deg, transparent, ${COLORS.primary})`,
};